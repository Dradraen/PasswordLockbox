<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_changepassword"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.deric.passwordlockbox.Changepassword">

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_alignParentTop="true"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="97dp"
        android:id="@+id/oldPass"
        android:hint="Old Password"
        android:textAlignment="center" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_below="@+id/oldPass"
        android:layout_alignEnd="@+id/oldPass"
        android:layout_marginTop="31dp"
        android:id="@+id/newPass"
        android:hint="New Password"
        android:textAlignment="center" />

    <EditText
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:inputType="textPassword"
        android:ems="10"
        android:layout_below="@+id/newPass"
        android:layout_alignEnd="@+id/newPass"
        android:layout_marginTop="33dp"
        android:id="@+id/newPassCon"
        android:hint="Confirm Password"
        android:textSize="18sp"
        android:textAlignment="center" />

    <Button
        android:text="Confirm"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@+id/newPassCon"
        android:layout_alignEnd="@+id/newPassCon"
        android:layout_marginTop="42dp"
        android:id="@+id/confirmBtn"
        android:onClick="confirmChange" />

    <Button
        android:text="Cancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignTop="@+id/confirmBtn"
        android:layout_alignStart="@+id/newPassCon"
        android:id="@+id/cancelBtn"
        android:onClick="cancelPasswordChange" />
</RelativeLayout>
